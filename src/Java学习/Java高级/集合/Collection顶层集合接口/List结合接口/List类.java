package Java学习.Java高级.集合.Collection顶层集合接口.List结合接口;

/**
 *一、extends Collection接口 共性方法
 *      1.add()      -- boolean
 *      2.remove(E e)  -- 返回被删除的元素 e
 *      3.size()
 *      4.toArray() 转化为数组
 *      5.isEmpty() 判断是否为空
 *      6.contains(E e) 判断是否包含
 *
 *二、特点：
 *      1.有序，就像队列一样，隧道，先进先出。
 *      2.有索引，包含一些带索引的方法
 *      3.允许重复的元素。
 * 三、特有的方法
 *      1.add(int index,E e) ,指定位置添加
 *      2.set(int index ,E e),指定位置修改，返回被修改的元素。
 */
public class List类 {
    public static void main(String[] args) {

    }
}
